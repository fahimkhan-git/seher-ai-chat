{
  "name": "homesfy-chat-bot",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/dashboard",
    "apps/widget"
  ],
  "scripts": {
    "dev:api": "npm --prefix apps/api run dev",
    "dev:dashboard": "npm --prefix apps/dashboard run dev",
    "build:widget": "npm --prefix apps/widget run build",
    "dev:widget": "npm --prefix apps/widget run dev",
    "test:local": "echo 'Starting local test environment...' && echo 'Terminal 1: npm run dev:api' && echo 'Terminal 2: npm run dev:widget' && echo 'Then open: test-widget-local.html'",
    "verify:build": "npm run build:widget && echo '✅ Widget build successful!'",
    "predeploy": "npm run verify:build",
    "migrate:file-to-mongo": "node scripts/migrate-file-store-to-mongo.mjs"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@remix-run/router": "^1.23.0"
  }
}
